extends: default

rules:
  line-length:
    max: 160
  document-start:
    present: false
  truthy:
    # GitHub Actions uses a bare `on:` trigger key
    check-keys: false
  comments:
    min-spaces-from-content: 1

ignore:
  - .claude/
